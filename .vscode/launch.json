{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python:调试old项目",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
