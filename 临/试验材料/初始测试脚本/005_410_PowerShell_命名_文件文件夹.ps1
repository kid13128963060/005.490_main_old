It looks like you provided a PowerShell script that was meant to be run from the command line, but it contains several errors. Let's correct them:

1. The script is intended for Python, but it starts with `python`, which is not valid in PowerShell.
2. There are missing quotes around some arguments.
3. The `--overwri` argument has an extra 'i' at the end.

Here's the corrected version of the PowerShell script: